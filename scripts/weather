#!/bin/bash

# Simple weather script - gets weather for a city
# Usage: weather [city]

set -e

CITY="${1:-San Francisco}"
API_URL="https://wttr.in/${CITY// /+}?format=3"

echo "Getting weather for: $CITY"
curl -s "$API_URL" || echo "Failed to get weather data"